# Production API: ONNX only — small image, fast CPU inference (Railway-friendly).
# Export: yolo export model=weights/best.pt format=onnx simplify=True imgsz=640
# Smaller/faster models (e.g. yolov8n / yolov10n) → smaller .onnx → faster inference.
onnxruntime>=1.17.0,<2
numpy>=1.24.0,<3
Pillow>=10.0.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
ultralytics>=8.1.0
